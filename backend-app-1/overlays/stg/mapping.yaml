apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: backend-app-1-mapping
  labels:
    localhost: localhost
spec:
  hostname: "127.0.0.1"
  prefix: "/api/"
  service: service.backend-app-1
  prefix_redirect: "/api/version"
